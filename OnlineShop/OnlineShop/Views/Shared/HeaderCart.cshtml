@model List<OnlineShop.Models.CartItem>
<div class="cart">
    <p>
        Chào mừng đến với cửa hàng trực tuyến của chúng tôi!! <span>Giỏ:</span><div id="dd" class="wrapper-dropdown-2">
            @Model.Count Số Lượnh(s)
            <ul class="dropdown">
                 @if(Model.Count > 0)
                {
                     <li>đả tồn tại</li>
                }
                else
                {
                   <li>bạn không có sản phẩm nào trong giở hàng</li>
                }
            </ul>
        </div>
    </p>
</div>

